const { checkArray, checkArrayEmpty } = require('./check-array');
const { checkBoolean } = require('./check-boolean');
const { checkNumber, checkNumberEmpty } = require('./check-number');
const { checkObjectId } = require('./check-objectid');
const { checkString, checkStringEmpty } = require('./check-string');

module.exports = {
  checkArray,
  checkArrayEmpty,
  checkBoolean,
  checkNumber,
  checkNumberEmpty,
  checkObjectId,
  checkString,
  checkStringEmpty
};
